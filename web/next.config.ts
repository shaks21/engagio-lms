import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  reactStrictMode: true,
  allowedDevOrigins: ['164.68.119.230'],
  outputFileTracingRoot: '/home/shaks/engagio-lms/web',
};

export default nextConfig;
