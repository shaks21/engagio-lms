/**
 * GREEN Phase: Teacher Heatmap Dashboard E2E
 *
 * Validates participant cards render with color-coded backgrounds
 * based on real-time engagement scores.
 */

import { test, expect } from '@playwright/test';

const BASE_URL = 'http://164.68.119.230:3001';

test.describe('Teacher Heatmap (GREEN)', () => {
  test('heatm...[truncated]